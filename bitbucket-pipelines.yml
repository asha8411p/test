pipelines:
  default:
    - step:
        name: Sync GitHub Mirror
        image: alpine/git:latest
        clone:
          enabled: false
        script:
          - git clone --bare git@bitbucket.org:asha8411p/test.git
          - cd test.git
          - git push --mirror git@github.com:asha8411p/test.git
